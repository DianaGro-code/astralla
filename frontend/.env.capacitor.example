# Copy this to .env.capacitor and fill in your Railway backend URL.
# This file is loaded when building the native iOS/Android app.
# It tells the app where to send API requests (can't use /api relative path in a native app).

VITE_API_URL=https://YOUR-BACKEND-NAME.up.railway.app/api
